Current repository statistics, excluding `docs/`, `node_modules/`, and `.git/`.

## Totals (as of 2025-11-03)
- Files: 546
- Lines (text files): 27,524
- Characters (text files): 1,034,429

## Refresh Locally
Run the helper script:

```bash
npm run stats
```

Notes
- Lines/characters include text-like files; common binary formats (fonts, images, archives, media) are skipped.
- File count includes all files except those inside the excluded folders above.
