import PublicReflectionComposeScreen from '../../src/screens/PublicReflectionComposeScreen'

// Compose a public (Shared) reflection — pushed from the Daily Word landing.
export default function DailyPublicReflection() {
  return <PublicReflectionComposeScreen />
}
