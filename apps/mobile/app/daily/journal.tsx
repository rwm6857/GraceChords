import ReflectionJournalScreen from '../../src/screens/ReflectionJournalScreen'

// The reflection journal — pushed from the Daily Word landing.
export default function DailyJournal() {
  return <ReflectionJournalScreen />
}
